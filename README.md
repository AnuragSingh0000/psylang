## Instructions to run

```bash
make run < INPUT_FILE
```

At the current stage, this will print out the result of lexing or report the corresponding lexing error. 


### To run the basic tests:

```
make run-tests
```

This runs the the lexer for psy programs in `tests` and saves them to  `lexer_results`