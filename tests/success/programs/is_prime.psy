fun isPrime(n: int) -> bool {
    if (n <= 1)
        ret false;

    for (var int i = 2; i * i <= n; i++)
        if (n % i == 0)
            ret false;

    ret true;
}

echo isPrime(17);
echo isPrime(20);